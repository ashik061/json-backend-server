{
  "name": "json-backend-server",
  "version": "1.0.0",
  "description": "A lightweight mock backend server that serves data from a JSON file, ideal for prototyping and testing frontend applications without needing a real backend.",
  "main": "index.js",
  "scripts": {
    "start": "npm run server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch db.json --port 8080"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "json-serve": "^0.1.0",
    "json-server": "^1.0.0-beta.1"
  }
}
